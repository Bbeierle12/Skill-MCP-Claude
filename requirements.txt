# Skills Manager Dependencies

# MCP Server
fastmcp>=0.1.0

# HTTP API Server
flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0

# Optional YAML frontmatter parsing
PyYAML>=6.0

# Canonical _meta.json schema validation (shared contract)
jsonschema>=4.0.0

# Build (optional)
pyinstaller>=6.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
